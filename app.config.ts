export default defineAppConfig({
  ui: {
    colors: {
      primary: 'mist',
      secondary: 'smoke',
      neutral: 'smoke',
      success: 'teal',
      info: 'cyan',
      warning: 'amber',
      error: 'rose'
    },
    button: {
      defaultVariants: {
        color: 'primary'
      }
    }
  }
})
